<p align="center">
  <a href="https://codember.dev" target="_blank">
    <img src="https://user-images.githubusercontent.com/68721455/200732795-dc1ecbfc-a77c-4f82-899a-6a9d632ab56d.png" width="100%" alt="Banner" />
  </a>
</p>

# 🤫 Secretos

<details>
    <summary>Last name</summary>
    <p>
        Hay una imagen borrosa donde aparece Guillermo Rauch. La respuesta es: <code>$ submit rauch</code>.
    </p>
</details>

<details>
    <summary>Confetti</summary>
    <p>Ejecutar múltiples veces el comando <code> $ confetti</code> hasta recibir el logro.</p>
</details>

<details>
    <summary>Konami code</summary>
    <p> Pulsar arriba, arriba, abajo, abajo, izquierda, derecha, izquierda, derecha, B, A.</p>
</details>

<details>
    <summary>Special date</summary>
    <p>
        Usando el comando <code>$ mail</code> puedes revisar los correos. 
        Si lees el primer correo con <code>$ mail 1</code>, te habla del comando para ver el calendario y 
        te pide que escribas la fecha señalada en él. La respuesta es: <code>$ submit 2023-12-01</code>.
    </p>
</details>

<details>
    <summary>Riddle</summary>
    <p>
        Usando el comando <code>$ mail</code> puedes revisar los correos. Si lees el segundo correo con <code>$ mail 2</code>, encontrarás un acertijo. La respuesta es: <code>$ submit html</code>.
    </p>
</details>

<details>
    <summary>It is magic!</summary>
    <p>
        Al leer el changelog.txt, tenemos una pista en la sección de los cambios de la versión 0.2.0 donde nos habla de la palabra mágica más famosa del mundo. Al escribir <code>$ abracadabra</code> nos aparecerá dibujado un mago en ASCII donde, además, nos dice que escribamos <code>$ submit itsmagic</code>.
    </p>
</details>

<details>
    <summary>Sauce of the year award 🥫</summary>
    <p>
        Usando el comando <code>$ mail</code> puedes revisar los correos. 
        Si lees el tercer correo con <code>$ mail 3</code>, te pregunta que si has completado el challenge
        3 porque a partir de los datos del mismo se puede obtener la contraseña para acceder con sudo a la carpeta privada. Se requiere la contraseña inválida número 13 del ejercicio. Al escribir <code>$ sudo nljzuyfzb</code> entraremos con el usuario miduGOD y podremos acceder a la carpeta privada. Una vez dentro de la misma, si vemos el archivo "ai.webp" mediante el comando <code>$ cat ai.webp</code> veremos una foto de Sam Altman. La respuesta es: <code>$ submit altman</code>.
    </p>
</details>

<details>
    <summary>You found the astronaut 🧑‍🚀</summary>
    <p>
        Usando el comando <code>$ mail</code> puedes revisar los correos. 
        Si lees el tercer correo con <code>$ mail 3</code>, te pregunta que si has completado el challenge 3 porque a partir de los datos del mismo se puede obtener la contraseña para acceder con sudo a la carpeta privada. Se requiere la contraseña inválida número 13 del ejercicio. Al escribir <code>$ sudo nljzuyfzb</code> entraremos con el usuario miduGOD y podremos acceder a la carpeta privada. Una vez dentro de la misma, si vemos el archivo "log_mars_mission.txt" mediante el comando <code>$ cat log_mars_mission.txt</code> podremos leer una historia sobre un astronauta y se menciona un comando. La respuesta es: <code>$ submit majortom</code>.
    </p>
</details>

<details>
    <summary>Barrel roll! 🛩️</summary>
    <p>
        Al leer el changelog.txt, tenemos una pista en la sección de los cambios de la versión 0.3.0 donde nos habla del nuevo comando 'do'. La respuesta es: <code>$ do a barrel roll</code>.
    </p>
</details>

<details>
    <summary>Ping pong 🏓!</summary>
    <p>
        Usando el comando <code>$ mail</code> puedes revisar los correos. 
        Si lees el tercer correo con <code>$ mail 4</code>, te aparece un correo escrito por una vaca de donde se puede intuir un comando nuevo. Si escribes <code>$ cowsay</code> en el terminal, te sugerirá que hagas un ping a midu.dev. Para desbloquear el secreto, ejecuta el comando <code>$ ping midu.dev</code>
    </p>
</details>

<details>
    <summary>Bug found 🐛!</summary>
    <p>
        Usando el comando <code>$ mail</code> puedes revisar los correos. 
        Si lees el tercer correo con <code>$ mail 5</code>, te aparece un correo con un nuevo acertijo. La respuesta es: <code>$ submit bug</code>
    </p>
</details>

<details>
    <summary>Ho ho ho 🎅!</summary>
    <p>
        Tal y como descubrimos en el secreto "You found the astronaut 🧑‍🚀", se puede acceder con sudo a la carpeta privada. Se requiere la contraseña inválida número 13 del ejercicio. Al escribir <code>$ sudo nljzuyfzb</code> entraremos con el usuario miduGOD y podremos acceder a la carpeta privada. Una vez dentro de la misma, si vemos el archivo "final_log_mars_mission.txt" mediante el comando <code>$ cat final_log_mars_mission.txt</code> podremos continuar la historia del astronauta y se menciona un comando. La respuesta es: <code>$ npm install adventjs</code>.
    </p>
</details>

<details>
    <summary>A Tetris in 40 minutes! ✅</summary>
    <p>
        En referencia a la famosa entrevista técnica, juega a tetris usando el comando <code>$ play tetris</code>
    </p>
</details>
